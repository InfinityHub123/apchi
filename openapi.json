{
  "components": {
    "schemas": {
      "Health": {
        "properties": {
          "mongo": {
            "title": "Mongo",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "status",
          "mongo"
        ],
        "title": "Health",
        "type": "object"
      }
    }
  },
  "info": {
    "summary": "Control plane for configuring one Trino cluster",
    "title": "Apchi",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/v1/health": {
      "get": {
        "operationId": "health_api_v1_health_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Health"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Apchi's own liveness",
        "tags": [
          "health"
        ]
      }
    }
  }
}
